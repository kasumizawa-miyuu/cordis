{
  "name": "cordis",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w @cordis/server\" \"npm run dev -w @cordis/client\"",
    "dev:client": "npm run dev -w @cordis/client",
    "dev:server": "npm run dev -w @cordis/server",
    "build": "npm run build -w @cordis/shared && npm run build -w @cordis/server && npm run build -w @cordis/client",
    "lint": "echo 'lint placeholder'",
    "typecheck": "npm run typecheck -w @cordis/shared && npm run typecheck -w @cordis/server && npm run typecheck -w @cordis/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}