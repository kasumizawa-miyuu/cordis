# SPEC_PROCESS.md — Spec & Plan Generation Process

## Brainstorming Key Nodes

### Node 1: 技术栈选型

AI 给出了三个选项：
- **A) TypeScript 全栈** — Node.js + React，生态成熟，与已有项目技术栈一致
- **B) Go 后端** — 并发性能好
- **C) Python 全栈** — 开发快但实时性能不如前两者

**决策：** 选择 A。理由：TypeScript 全栈从 DB 到 UI 类型贯通，Socket.IO 生态成熟，npm workspaces 支持 monorepo 无需额外工具。

### Node 2: 前端框架

AI 建议 React，理由是 Open Design 对 React 支持最成熟，且社区资源最丰富。

**决策：** 采纳。React + Vite + React Router v6。

### Node 3: 数据库选型

AI 建议 PostgreSQL，理由是聊天室数据（用户-房间-消息）天然是关系型结构，且 Render 原生支持 PostgreSQL。

**决策：** 采纳。Prisma ORM 提供类型安全的数据库访问。

### Node 4: 插件系统架构

这是最关键的决策。AI 最初提出三种方案：
- **A) 插件系统** — 聊天室定义标准接口，扩展作为独立插件注册
- **B) iframe/微前端嵌入** — 扩展通过 iframe 嵌入
- **C) 纯 API 驱动** — 只提供 REST/WebSocket API

**修正：** 我推翻了 AI 最初的"插件在聊天室内运行"的设计。我明确插件应该是完全独立的应用，聊天室只是"启动器"——提供房间信息和成员信息，锁定房间，通知成员跳转。插件完全在自己的页面中运行，与聊天室核心解耦。AI 根据这个反馈重新设计了接口，将 `IPluginContext` 简化为 `IPluginLaunchContext`，插件通过临时 token 获取上下文。

### Node 5: 登录认证方式

AI 给出三个选项：仅邮箱+密码、邮箱+OAuth、仅 OAuth。我选择邮箱+密码，但补充了关键要求：邮箱验证通过自动化邮件发送验证码，支持密码重置。AI 据此设计了完整的 EmailVerification 实体和 Nodemailer 集成。

### Node 6: 功能范围界定

AI 将功能分为核心 MVP 和可选扩展。我纠正了两点：
- 邀请系统属于核心 MVP（AI 原将其放在可选扩展中）
- 补充了准备系统和房间锁定功能

AI 随后系统化整理了 10 个功能模块，涵盖了所有需求。

## 关键迭代记录

### 迭代 1: Room.metadata 的扩展性理解

**AI 最初设计：** Room 有一个 `metadata` JSON 字段，用于存储房主自定义的扩展属性（如置顶、主题色等）。

**我的修正：** 扩展性应该是开发者层面的，方便开发者根据需求调整功能，而不是让房主随意塞 key-value。房主能设置什么由软件功能决定。

**结果：** 删除了 `Room.metadata`，明确 Room 的属性是软件明确定义的字段。扩展性体现在代码架构和插件接口层面。

### 迭代 2: 插件系统从"宿主"到"启动器"

**AI 最初设计：** 插件在聊天室内部运行，通过 `IPluginContext` 与房间交互，有一个 `panelComponent` 在房间侧边栏展示。

**我的修正：** 插件是独立的功能模块，聊天室只提供房间和成员信息。用户启动插件后，每个用户新开一个网页，由聊天室调起，但插件内的所有操作与聊天室无关。插件结束后，聊天室解锁。

**结果：** AI 重新设计了插件接口，将架构从"嵌入式面板"改为"独立应用启动器"。插件通过 REST API 与 Cordis 通信，使用临时 token 验证。

### 迭代 3: 房间持久化

**补充要求：** 房间是持久化的，与创建者无关。房主不在时，成员仍可进入聊天，但不能启动插件。只有房主可删除房间，房主可转让。

**结果：** AI 在 RoomService 中实现了这些约束：leave 方法检查是否为 owner，transferOwnership 使用事务确保原子性。

## 用户故事的 INVEST 检查

AI 最初提出了 8 个用户故事，覆盖了注册登录、浏览房间、创建管理、实时聊天、邀请、准备与插件、历史消息、密码重置。每个故事都独立、可协商、有价值、可估算、小而可测试。

所有用户故事均被采纳，无修改。

## 反思：brainstorming 技能的表现

**做得好的地方：**
- 追问质量高：每个关键决策点都给出了 2-3 个选项和理由，帮助我快速做出选择
- 功能清单系统化：在我给出零散需求后，AI 将其整理为 10 个清晰的功能模块
- 凭据威胁模型分析全面：从 JWT 泄露到 SMTP 密码到数据库密码，覆盖了全链路

**不满的地方：**
- 插件系统的最初设计方向与我设想差距较大，需要我明确纠正后才调整
- 对"房间属性扩展性"的理解过于工程化，偏离了实际需求
- 部分设计细节（如具体组件命名）在 brainstorm 阶段就过细，应在 plan 阶段决定