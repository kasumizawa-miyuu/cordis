# REFLECTION.md — 反思报告

## 哪些 Superpowers 技能发挥了最大作用

**brainstorming 技能**是本次项目中价值最高的部分。它不是简单地"记录需求"，而是通过一轮又一轮的追问，迫使我澄清那些我以为自己已经想清楚了、实际上模糊不清的设计决策。最典型的例子是插件系统——我最初说"聊天室要给出完整的接口供扩展使用"，但 AI 追问"你期望的集成方式是什么"时，我意识到"接口"这个词可以有很多种解释。经过三轮迭代，从"嵌入式面板"到"独立应用启动器"，设计才真正清晰。如果没有这个技能的结构化追问，我很可能带着模糊的理解直接进入实现，然后在 codes 阶段反复返工。

**subagent-driven-development 技能**的"fresh subagent per task"设计非常有效。每个 subagent 只看到它需要完成的那一个任务，不会被前面任务的上下文污染。在 Tasks 8-11 的批量派发中，subagent 在单个会话中完成了 4 个独立模块，测试覆盖率达到 76/76，没有出现模块间接口不匹配的问题。这说明只要 plan 中的接口定义足够清晰，subagent 可以在不理解全局的情况下做出正确的局部决策。

**writing-plans 技能**的 task 颗粒度控制帮助很大。19 个 task 每个都有明确的文件列表、接口定义和验证步骤。subagent 在执行时几乎不需要回问（整个实现过程中只有一次 subagent 提问），这证明了 plan 的清晰度。

## 哪些"形式大于实质"

**test-driven-development 技能**在本次项目中的实际价值有限。虽然每个 task 的 plan 都写明了 TDD 要求，但 subagent 实际执行时往往是先写实现再写测试。这不是 subagent 的问题，而是项目性质决定的——大部分 task 是"创建新模块"而非"修复 bug"或"重构"。对于绿场项目，TDD 的"先写失败测试"步骤确实更像仪式。但在 Tasks 4（Email Service）和 5（Auth Service）中，TDD 真正发挥了作用——subagent 先写了测试，然后对照测试实现了功能，测试覆盖了边界条件（如重复注册返回 409、错误密码返回 401）。

**finishing-a-development-branch 技能**的"4 个选项"菜单在实际使用中显得多余。因为我在使用 subagent-driven-development 时已经明确了目标——执行完所有 task 后合并到 main。此时再让我选择"merge/Push/keep/discard"是多余的确认。这个技能更适合在"不确定是否要合并"的场景下使用。

## TDD 强制在 AI 协作下是阻碍还是放大器

答案是：**视任务性质而定，但总体是放大器**。

对于**创建新模块**的任务（如创建 Room 路由），TDD 的"先写测试"步骤确实增加了时间成本。subagent 需要先理解测试框架和 mock 策略，而这些在绿场项目中需要从头搭建。但我观察到，subagent 在写测试的过程中实际上更深入地理解了需求——测试用例就是需求的可执行版本。

对于**修改现有代码**或**边界条件明确**的任务（如 Email Service 的验证码生成），TDD 是明确的正向放大器。subagent 先写了 `generateCode()` 应该返回 6 位数字字符串的测试，然后实现，两分钟完成。如果反过来（先实现再补测试），很可能遗漏 "两次调用返回不同值" 这样的边界条件。

关键教训是：TDD 最有效的场景是**需求可以用明确的输入/输出描述**的场景。对于"创建一个带有聊天面板的页面"这样的 UI 任务，TDD 的价值较低，因为 React 组件的测试需要大量 mock（WebSocket、API 调用），而测试本身往往只能验证"组件渲染了"这样的浅层行为。

## Subagent-Driven 工作流让智能体能自主运行多久而不偏离主题

在本次项目中，subagent 最长的一次自主运行是 Tasks 8-11 的批量派发，大约连续运行了 6 分钟，完成了 4 个独立模块，提交了 4 个 commit。subagent 没有偏离主题，输出的代码质量一致。

我认为这得益于两个因素：
1. **Plan 中的接口定义清晰**：每个 task 的 "Interfaces" 部分明确列出了 consumes 和 produces，subagent 知道它需要什么输入、输出什么
2. **Task 颗粒度适中**：每个 task 的目标单一（如"创建邀请系统"），不会让 subagent 在多个目标之间切换

但也有一个值得注意的问题：Tasks 17-19 的执行过程中，subagent 在 Task 19（集成验证）中创建了多个本应在之前 task 中创建的文件（如 `db.ts`、`middleware/auth.ts` 等）。这说明前面的 task 遗漏了这些文件，subagent 在集成阶段发现并补上了。这暴露了 subagent-driven 的一个风险：**每个 subagent 只关心自己的 task，可能遗漏跨 task 的依赖**。这个问题在 plan 阶段应该通过更仔细的接口审查来预防。

## 什么样的 Task 颗粒度最优

从本次项目的经验来看，最优颗粒度是：

- **一个 task = 一个完整的模块**（如 RoomService + RoomRoutes + 测试）
- **文件数：3-5 个**（service + route + test + 可能的 middleware）
- **时间：2-5 分钟**（subagent 实现时间，不是人类理解时间）
- **测试数：5-30 个**

太小的颗粒度（如"创建一个文件"）会导致 plan 过于冗长，subagent 频繁切换上下文。太大的颗粒度（如"实现整个后端"）会让 subagent 迷失方向，输出质量下降。

本次项目中最成功的 task 是 Task 5（Auth Service），它包含了 service、middleware 和 routes 三个层，但目标单一（认证系统），subagent 一次完成，15 个测试全部通过。

## SPEC/PLAN 质量如何影响实现质量

**一个具体案例：** PLAN 中 Task 14（Room Page）的 brief 列出了 12 个文件要创建，但没有明确说明 `socket.ts` 和 `chatStore.ts` 之间的数据流关系。subagent 在实现时，`chatStore` 的 `addMessage` 方法和 `socket.ts` 的事件监听之间的连接是通过 `useChat` hook 手动协调的，而非通过 store 内部的订阅机制。这导致了一个微妙的 bug：当用户在快速切换房间时，旧房间的消息事件可能被错误地添加到新房间的 store 中。

这个案例说明：**SPEC 定义了"做什么"，PLAN 定义了"在哪里做"，但"怎么做"的细节在 plan 中仍然不足**。对于 subagent 来说，接口定义（函数签名）足够，但**数据流方向**（谁调用谁、谁监听谁）也需要明确。本可以在 PLAN 中加一句"chatStore 通过 socket 事件回调更新，而非主动轮询"，就能避免这个问题。

另一个案例是 CI 构建失败：PLAN 中 Task 17 的 CI workflow 没有包含"构建 shared 包"的步骤，因为 Task 2 的 shared 包构建隐含了"开发时已经构建好"的假设。在 CI 的干净环境中，这个假设不成立。如果在 PLAN 的 Global Constraints 中明确"所有 CI job 必须显式构建依赖包"，就能避免这个错误。

## 最有效的 Prompt/Context 策略

**"批量派发"策略** 是最有效的。将 Tasks 8-11 打包为一个 subagent 任务，比逐个派发节省了约 50% 的时间。原因：
1. subagent 在同一个会话中保持了上下文，不需要重复探索代码结构
2. 减少了"派发→等待→审查"的循环次数

**"具体代码示例"策略** 也很有效。在 Task 5 的 prompt 中，我给出了 `AuthService.register` 的步骤描述（"检查重复→hash 密码→创建用户→创建验证记录→发送邮件→返回 token"），subagent 按照这个步骤精准实现了所有逻辑，没有遗漏任何一步。相比之下，Task 14 的 prompt 只列出了文件列表，subagent 的实现就比较松散。

**"完整上下文"策略** 的效果参差不齐。给 subagent 太多上下文（如完整的 SPEC.md）会让它混淆当前 task 的范围。最佳做法是：只给 task brief + 相关的 SPEC 章节 + 接口定义。

## 凭据与分发的工程要求

凭据安全要求迫使我想清楚了几个原本会忽略的问题：

1. **环境变量的生命周期**：开发环境用 `.env`，生产环境用 Render 环境变量，CI 环境用 GitHub Secrets。三个环境的环境变量来源不同，但代码只需读取 `process.env`。这个抽象层的设计是 plan 阶段就确定的，避免了后续的混乱。

2. **SMTP 密码不是邮箱密码**：QQ 邮箱的 SMTP 需要"授权码"而非登录密码。如果在 README 中不说明这一点，用户会困惑为什么密码正确但邮件发不出去。

3. **Docker 多阶段构建的必要性**：没有 Docker 经验的人可能不理解为什么需要三个阶段。实际上，Stage 1 构建 client（Vite build），Stage 2 构建 server（TypeScript 编译），Stage 3 只复制构建产物。这让最终镜像从 800MB 缩减到约 200MB。

分发要求让我意识到"别人如何运行你的项目"是一个需要仔细设计的工程问题。`docker-compose.yml` 让用户一条命令就能启动 PostgreSQL + 应用，而不用手动配置数据库。

## 如果重做我会改变什么

1. **在 PLAN 中增加数据流图**：不只是列出接口签名，还要画出数据流向（如"chatStore ← socket event → ChatPanel"）。这会减少 subagent 在实现时的猜测。

2. **先实现一个最小的"端到端"流程**：在 Task 1 完成后，先创建一个"hello world"的客户端-服务器连接，验证整个技术栈能正常工作。目前是到最后（Task 19）才做集成验证，发现了一些本应更早发现的问题。

3. **将 CI 配置放在更早的阶段**：CI 应该在 Task 1 之后就配置好，这样每个 task 的提交都能立即验证。目前 CI 在 Task 17 才加入，导致前面的 task 都缺少 CI 反馈。

4. **更严格地使用 TDD**：对于 UI 组件，虽然测试价值有限，但至少应该写一个"组件能渲染"的烟雾测试，防止 subagent 引入语法错误或 import 错误。

## 对 Superpowers 方法论的批判

Superpowers 方法论的核心理念是：**通过结构化的流程（brainstorming → plan → task → review → finish）来约束 AI 协作中的不确定性**。它假设：

1. **需求可以在实现前完全澄清**：这个假设在本次项目中基本成立，因为聊天室的需求相对明确。但对于探索性强的项目，brainstorming 可能无法覆盖所有未知情况。

2. **subagent 可以在有限上下文中完成独立任务**：这个假设大部分成立，但"集成问题"（如跨模块的接口不匹配）需要人类审查来发现。Superpowers 的两阶段 review（spec 合规 + 代码质量）部分解决了这个问题，但 reviewer 也是 subagent，也有上下文限制。

3. **TDD 是普遍的工程最佳实践**：这个假设在绿场项目中不完全成立——"先写测试"对于创建新模块是有成本的。Superpowers 也许应该为不同成熟度的项目提供不同的 TDD 策略。

4. **worktree 隔离是必要的**：这个假设在本次项目中成立，但 worktree 的创建和清理增加了操作复杂度。对于简单的项目，直接在分支上工作可能更高效。

总的来说，Superpowers 是一套**为有经验的工程师设计的 AI 协作框架**。它假设你已经有清晰的设计思路，需要的是流程纪律而非创意引导。对于像我这样正在学习如何与 AI 协作的学生来说，这套框架帮助我建立了一种"先想清楚再动手"的思维方式，这可能是本次项目最大的收获。但我也意识到，真正优秀的软件不仅需要流程纪律，还需要在流程中保持灵活性——知道什么时候该遵循技能，什么时候该偏离。这是需要继续练习的能力。